%h1.title TASK LIST
%table
  %thead
    %tr
      %th
        STATE
      %th
        タスク名
      %th
        期限
  %tbody
    - @tasks.each do |task|
      %tr
        %td
          = task.state
        %td
          = task.task
        %td
          = task.limit_date
= link_to '追加', new_tasks_path
